<% @posts.each do |post| %>
<div>
  <div>
    <p><%= post.title %></p>
    <p><%= post.content %></p>
    <span>
      <%= link_to "show post", post_path(post)%>
    </span>
  </div>
</div>
<% end %>
